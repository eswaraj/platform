<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context" xmlns:util="http://www.springframework.org/schema/util"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.1.xsd">


	<context:component-scan base-package="com.eswaraj.tasks.topology" />

	<bean class="com.eswaraj.tasks.topology.SpringEswarajTopology">
		<property name="name" value="Eswaraj" />
		<property name="maxSpoutPending" value="5000" />
		<property name="numWorkers" value="20" />
		<property name="numParallel" value="20" />

		<property name="spoutConfigs">
			<list>
				<ref bean="CatageoryChangeSpoutBean" />
				<ref bean="LocationFileUploadSpoutBean" />
				<ref bean="ComplaintCreatedSpoutBean" />
			</list>
		</property>
		<property name="boltConfigs">
			<list>
				<ref bean="CatageoryChangeBoltBean" />
				<ref bean="LocationFileDistributeBoltBean" />
				<ref bean="LocationOneFileProcessorBoltBean" />
				<ref bean="GlobalHourlyCounterBoltBean" />

			</list>
		</property>
	</bean>

	<bean id="CatageoryChangeSpoutBean" class="com.eswaraj.tasks.spout.CatageoryChangeSpout">
		<property name="componentId" value="CatageoryChangeSpout" />
		<property name="outputStream" value="CatageoryChangeStream" />
		<!-- Properties define which services will be available for spout -->
		<property name="initializeRedisServices" value="false" />
		<property name="initializeQueueServices" value="true" />
		<property name="initializeDbServices" value="false" />

		<!-- Redis -->
		<property name="redisUrl" value="${redis_server}" />
		<property name="redisPort" value="${redis_port}" />
		<!-- Neo4j -->
		<property name="dbUrl" value="${db_url}" />
		<!-- Aws Queue -->
		<property name="regions" value="${aws_region}" />
		<property name="accessKey" value="${aws_access_key}" />
		<property name="secretKey" value="${aws_access_secret}" />

		<!-- Queue Names -->
		<property name="awsLocationQueueName" value="${aws_location_file_queue_name}" />
		<property name="awsCategoryUpdateQueueName" value="${aws_category_queue_name}" />
		<property name="awsComplaintCreatedQueueName" value="${aws_complaint_created_queue_name}" />
	</bean>
	<bean id="LocationFileUploadSpoutBean" class="com.eswaraj.tasks.spout.LocationFileUploadSpout">
		<property name="componentId" value="LocationFileUploadSpout" />
		<property name="outputStream" value="LocationFileUploadStream" />
		<!-- Properties define which services will be available for spout -->
		<property name="initializeRedisServices" value="false" />
		<property name="initializeQueueServices" value="true" />
		<property name="initializeDbServices" value="false" />

		<!-- Redis -->
		<property name="redisUrl" value="${redis_server}" />
		<property name="redisPort" value="${redis_port}" />
		<!-- Neo4j -->
		<property name="dbUrl" value="${db_url}" />
		<!-- Aws Queue -->
		<property name="regions" value="${aws_region}" />
		<property name="accessKey" value="${aws_access_key}" />
		<property name="secretKey" value="${aws_access_secret}" />
		<!-- Queue Names -->
		<property name="awsLocationQueueName" value="${aws_location_file_queue_name}" />
		<property name="awsCategoryUpdateQueueName" value="${aws_category_queue_name}" />
		<property name="awsComplaintCreatedQueueName" value="${aws_complaint_created_queue_name}" />
	</bean>

	<bean id="ComplaintCreatedSpoutBean" class="com.eswaraj.tasks.spout.ComplaintCreatedSpout">
		<property name="componentId" value="ComplaintCreatedSpout" />
		<property name="outputStream" value="ComplaintCreatedStream" />
		
		<!-- Properties define which services will be available for spout -->
		<property name="initializeRedisServices" value="false" />
		<property name="initializeQueueServices" value="true" />
		<property name="initializeDbServices" value="false" />

		<!-- Redis -->
		<property name="redisUrl" value="${redis_server}" />
		<property name="redisPort" value="${redis_port}" />
		<!-- Neo4j -->
		<property name="dbUrl" value="${db_url}" />
		<!-- Aws Queue -->
		<property name="regions" value="${aws_region}" />
		<property name="accessKey" value="${aws_access_key}" />
		<property name="secretKey" value="${aws_access_secret}" />

		<!-- Queue Names -->
		<property name="awsLocationQueueName" value="${aws_location_file_queue_name}" />
		<property name="awsCategoryUpdateQueueName" value="${aws_category_queue_name}" />
		<property name="awsComplaintCreatedQueueName" value="${aws_complaint_created_queue_name}" />

	</bean>

	<bean id="CatageoryChangeBoltBean" class="com.eswaraj.tasks.bolt.SaveCategoryBolt">
		<property name="componentId" value="CatageoryChangeBolt" />
		<!-- Properties define which services will be available for Bolt -->
		<property name="initializeRedisServices" value="true" />
		<property name="initializeQueueServices" value="false" />
		<property name="initializeDbServices" value="true" />
		<property name="sourceComponentStreams">
			<map>
				<entry key="CatageoryChangeSpout" value="CatageoryChangeStream" />
			</map>
		</property>

		<!-- Redis -->
		<property name="redisUrl" value="${redis_server}" />
		<property name="redisPort" value="${redis_port}" />
		<!-- Neo4j -->
		<property name="dbUrl" value="${db_url}" />
		<!-- Aws Queue -->
		<property name="regions" value="${aws_region}" />
		<property name="accessKey" value="${aws_access_key}" />
		<property name="secretKey" value="${aws_access_secret}" />

		<!-- Queue Names -->
		<property name="awsLocationQueueName" value="${aws_location_file_queue_name}" />
		<property name="awsCategoryUpdateQueueName" value="${aws_category_queue_name}" />
		<property name="awsComplaintCreatedQueueName" value="${aws_complaint_created_queue_name}" />

	</bean>


	<bean id="LocationFileDistributeBoltBean" class="com.eswaraj.tasks.bolt.LocationFileDistributeBolt">
		<property name="componentId" value="LocationFileDistributeBolt" />
		<property name="outputStream" value="LocationFileDistributeStream" />
		<!-- Properties define which services will be available for Bolt -->
		<property name="initializeRedisServices" value="true" />
		<property name="initializeQueueServices" value="false" />
		<property name="initializeDbServices" value="true" />
		<property name="sourceComponentStreams">
			<map>
				<entry key="LocationFileUploadSpout" value="LocationFileUploadStream" />
			</map>
		</property>

		<!-- Redis -->
		<property name="redisUrl" value="${redis_server}" />
		<property name="redisPort" value="${redis_port}" />
		<!-- Neo4j -->
		<property name="dbUrl" value="${db_url}" />
		<!-- Aws Queue -->
		<property name="regions" value="${aws_region}" />
		<property name="accessKey" value="${aws_access_key}" />
		<property name="secretKey" value="${aws_access_secret}" />

		<!-- Queue Names -->
		<property name="awsLocationQueueName" value="${aws_location_file_queue_name}" />
		<property name="awsCategoryUpdateQueueName" value="${aws_category_queue_name}" />
		<property name="awsComplaintCreatedQueueName" value="${aws_complaint_created_queue_name}" />

	</bean>

	<bean id="LocationOneFileProcessorBoltBean" class="com.eswaraj.tasks.bolt.LocationOneFileProcessorBolt">
		<property name="componentId" value="LocationOneFileProcessorBolt" />
		<property name="paralellism" value="20" />
		<!-- Properties define which services will be available for Bolt -->
		<property name="initializeRedisServices" value="true" />
		<property name="initializeQueueServices" value="false" />
		<property name="initializeDbServices" value="true" />
		<property name="sourceComponentStreams">
			<map>
				<entry key="LocationFileDistributeBolt" value="LocationFileDistributeStream" />
			</map>
		</property>

		<!-- Redis -->
		<property name="redisUrl" value="${redis_server}" />
		<property name="redisPort" value="${redis_port}" />
		<!-- Neo4j -->
		<property name="dbUrl" value="${db_url}" />
		<!-- Aws Queue -->
		<property name="regions" value="${aws_region}" />
		<property name="accessKey" value="${aws_access_key}" />
		<property name="secretKey" value="${aws_access_secret}" />

		<!-- Queue Names -->
		<property name="awsLocationQueueName" value="${aws_location_file_queue_name}" />
		<property name="awsCategoryUpdateQueueName" value="${aws_category_queue_name}" />
		<property name="awsComplaintCreatedQueueName" value="${aws_complaint_created_queue_name}" />

	</bean>

	<bean id="GlobalHourlyCounterBoltBean" class="com.eswaraj.tasks.bolt.GlobalHourlyCounterBolt">
		<property name="componentId" value="GlobalHourlyCounterBolt" />
		<property name="keyPrefix" value="Global" />
		<property name="keyPrefixFieldName" value="keyPrefixFieldName" />
		
		<property name="paralellism" value="1" />
		<!-- Properties define which services will be available for Bolt -->
		<property name="initializeRedisServices" value="true" />
		<property name="initializeQueueServices" value="false" />
		<property name="initializeDbServices" value="true" />
		<property name="sourceComponentStreams">
			<map>
				<entry key="ComplaintCreatedSpout" value="ComplaintCreatedStream" />
			</map>
		</property>

		<!-- Redis -->
		<property name="redisUrl" value="${redis_server}" />
		<property name="redisPort" value="${redis_port}" />
		<!-- Neo4j -->
		<property name="dbUrl" value="${db_url}" />
		<!-- Aws Queue -->
		<property name="regions" value="${aws_region}" />
		<property name="accessKey" value="${aws_access_key}" />
		<property name="secretKey" value="${aws_access_secret}" />

		<!-- Queue Names -->
		<property name="awsLocationQueueName" value="${aws_location_file_queue_name}" />
		<property name="awsCategoryUpdateQueueName" value="${aws_category_queue_name}" />
		<property name="awsComplaintCreatedQueueName" value="${aws_complaint_created_queue_name}" />

	</bean>



	<!-- <bean id="LocotionFileProcessorBoltBean" class="com.eswaraj.tasks.bolt.LocationFileProcessorBolt"> <property name="componentId" value="LocationFileProcessorBolt" /> <property name="initializeRedisServices" 
		value="true" /> <property name="initializeQueueServices" value="false" /> <property name="initializeDbServices" value="true" /> <property name="sourceComponentStreams"> <map> <entry key="LocationFileUploadSpout" 
		value="LocationFileUploadStream" /> </map> </property> <property name="redisUrl" value="${redis_server}" /> <property name="redisPort" value="${redis_port}" /> <property name="dbUrl" value="${db_url}" 
		/> <property name="regions" value="${aws_region}" /> <property name="accessKey" value="${aws_access_key}" /> <property name="secretKey" value="${aws_access_secret}" /> <property name="awsLocationQueueName" 
		value="${aws_location_file_queue_name}" /> <property name="awsCategoryUpdateQueueName" value="${aws_category_queue_name}" /> <property name="awsComplaintCreatedQueueName" value="${aws_complaint_created_queue_name}" 
		/> </bean> -->
	<bean id="propertyPlaceholderConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
		<property name="searchSystemEnvironment" value="true" />
		<property name="properties">
			<util:properties>
				<prop key="author">Ravi Sharma</prop>
			</util:properties>
		</property>
	</bean>

</beans>